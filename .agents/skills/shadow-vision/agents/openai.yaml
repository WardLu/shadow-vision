interface:
  display_name: "Shadow Vision"
  short_description: "Repo-specific patterns and workflows for shadow-vision"
  default_prompt: "Use the shadow-vision repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true